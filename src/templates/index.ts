/**
 * Templates
 * Pre-configured health check setups for common use cases
 */

export { defaultTemplate } from './default';
export { contentfulSiteTemplate } from './contentful-site';
export { minimalTemplate } from './minimal';
