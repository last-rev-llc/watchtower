---
"@last-rev/watchtower": patch
---

Add Datadog synthetics templates and reference sync script to `examples/datadog-synthetics/`. Generic P1/P2 JSON definitions plus a dependency-free `sync.js` script that consumers can copy into their own CI to keep synthetics in source control.
